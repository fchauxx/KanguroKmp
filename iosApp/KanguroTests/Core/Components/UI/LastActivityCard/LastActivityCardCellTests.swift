@testable import Kanguro
import XCTest
import SwiftUI

class LastActivityCardCellTests: XCTestCase {
    
    // MARK: - Stored Properties
    var lastActivityCardCell: LastActivityCardCell!
    
    // MARK: - Initializers
    override func setUp() {
        continueAfterFailure = false
        lastActivityCardCell = LastActivityCardCell()
    }
}

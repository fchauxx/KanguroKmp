import Foundation

public enum KanguroBoolFeatureFlagKeys: String {
    case shouldShowRenters
    case shouldUseOTPValidation
    case shouldShowLiveVet
}

public enum KanguroStringFeatureFlagKeys: String {
    case stringDummy // delete it as soon a real key arrives
}

public enum KanguroIntFeatureFlagKeys: String {
    case intDummy // delete it as soon a real key arrives
}

public enum KanguroFloatFeatureFlagKeys: String {
    case floatDummy // delete it as soon a real key arrives
}

public enum KanguroDictionaryFeatureFlagKeys: String {
    case dictDummy // delete it as soon a real key arrives
}

public enum KanguroDataFeatureFlagKeys: String {
    case dataDummy // delete it as soon a real key arrives
}

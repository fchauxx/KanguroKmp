import XCTest
@testable import KanguroRentersDomain

final class KanguroRentersDomainTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(KanguroRentersDomain().text, "Hello, World!")
    }
}
